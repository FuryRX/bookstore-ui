apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookstore-ui.fullname" . }}
  labels:
    {{- include "bookstore-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookstore-ui.selectorLabels" . | nindent 4 }}
